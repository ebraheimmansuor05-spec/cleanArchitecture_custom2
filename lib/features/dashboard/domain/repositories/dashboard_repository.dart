/// Provider-independent boundary for future Dashboard aggregation operations.
///
/// Operations belong here only when their business meaning and owning feature
/// contracts are explicitly defined. F03 currently has no domain data calls.
abstract interface class DashboardRepository {}
